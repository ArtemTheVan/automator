#ifndef KEYBOARD_H_
#define KEYBOARD_H_

#include <windows.h>
#include <stdio.h>
#include <string.h>

// Симуляция нажатия клавиш для строки (включая спецсимволы)
void simulate_keystroke(const char *text);

#endif
